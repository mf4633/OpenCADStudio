# OpenCADStudio specific (in addition to root .gitignore)
target/
debug/
release/
Cargo.lock   # committed for reproducibility in apps, but safe to ignore if preferred

# Large assets sometimes generated during development
*.pdb
*.exe
*.dll
*.so
*.dylib

# IDE
.idea/
.vscode/
*.swp

# Per review hygiene: ensure no committed build artifacts or secrets

